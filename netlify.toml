[build]
  command = "vite build"
  functions = "functions/"
  publish = "dist"

[dev]
  framework = "vite"
  command = "vite"
  targetPort = 5173
  port = 8888
  [dev.https]
    certFile = "./ssl/localhost.crt"
    keyFile = "./ssl/localhost.key"